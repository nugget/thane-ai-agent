# Thane Configuration
# Generated by: go generate ./internal/config/gen/gencfg
# Do not edit manually — update example.go and re-run go generate.
#
# Config search order:
#   ./config.yaml, ~/Thane/config.yaml, ~/.config/thane/config.yaml,
#   /config/config.yaml, /usr/local/etc/thane/config.yaml, /etc/thane/config.yaml
# Override: thane -config /path/to/config.yaml serve
#
# Environment variables are expanded (e.g., ${HOME}).
# Protect secrets: chmod 600 config.yaml
#
# Sections marked with '# (optional)' are commented out.
# Uncomment and configure as needed.

# Listen configures the primary HTTP API server (OpenAI-compatible).
listen:
  # Address is the network address to bind to. Empty string means
  # all interfaces (0.0.0.0).
  address: ""
  # Port is the TCP port to listen on. Default: 8080.
  port: 8080
# OllamaAPI configures the optional Ollama-compatible API server,
# used for Home Assistant integration.
ollama_api:
  enabled: true
  address: ""
  port: 11434
#
# (optional) CardDAV configures the optional CardDAV server for native
# carddav:
#   enabled: true
#   listen:
#     - 127.0.0.1:8843
#   username: thane
#   password: your-carddav-password
#
# HomeAssistant configures the connection to a Home Assistant instance.
homeassistant:
  url: https://your-homeassistant.local:8123
  token: your-long-lived-access-token
  # Subscribe configures WebSocket event subscriptions for real-time
  # state change monitoring. When entity_globs is non-empty, only
  # matching entities are processed; when empty, all state changes
  # are accepted.
  subscribe:
    # EntityGlobs is a list of glob patterns (using path.Match syntax)
    # that select which entity IDs to process. Examples: "person.*",
    # "binary_sensor.*door*", "light.living_room". An empty list
    # means all entities are accepted.
    entity_globs:
      - person.*
      - binary_sensor.*door*
    # RateLimitPerMinute caps how many state changes per entity are
    # forwarded per minute. Zero means no rate limiting.
    rate_limit_per_minute: 10
    # CooldownMinutes is the per-anticipation cooldown period in minutes.
    # After an anticipation triggers a wake, it cannot trigger again until
    # this interval elapses. Defaults to 5 minutes via applyDefaults.
    cooldown_minutes: 5
# Models configures LLM providers, model routing, and the default model.
models:
  # Default is the model name used when no specific model is requested.
  default: qwen2.5:72b
  # OllamaURL is the base URL of the Ollama API server used as the
  # default LLM backend. Default: "http://localhost:11434".
  ollama_url: http://your-ollama-server:11434
  # LocalFirst prefers local (cost_tier=0) models over cloud models
  # when routing decisions are made by the model router.
  local_first: true
  # RecoveryModel is a fast, cheap model used to generate summaries
  # when the primary model times out after completing tool calls.
  # When empty, timeout recovery falls back to a static message
  # listing the tools that were used.
  recovery_model: qwen3:4b
  # Available lists all models that Thane can route to. Each entry
  # maps a model name to a provider and declares its capabilities.
  available:
    - name: qwen3:4b
      provider: ollama
      supports_tools: true
      context_window: 32768
      speed: 9
      quality: 5
      cost_tier: 0
      min_complexity: simple
    - name: qwen2.5:72b
      provider: ollama
      supports_tools: true
      context_window: 131072
      speed: 3
      quality: 9
      cost_tier: 0
      min_complexity: moderate
#
# (optional) Anthropic configures the Anthropic (Claude) API provider.
# anthropic:
#   api_key: sk-ant-your-api-key
#
# Embeddings configures vector embedding generation for semantic search.
embeddings:
  enabled: false
  model: nomic-embed-text
  baseurl: ""
# Workspace configures the agent's sandboxed file system access.
workspace:
  # Path is the root directory for file operations. If empty, file
  # tools are disabled entirely.
  path: ""
  # ReadOnlyDirs are additional directories the agent can read from
  # but not write to. Useful for giving the agent access to reference
  # material outside its workspace.
  read_only_dirs: []
# Paths maps named prefixes to directory paths for file resolution.
# Each entry creates a prefix (e.g., "kb" → kb:path resolves to
# the configured directory). Supports ~ expansion at resolver
# construction time.
paths:
  kb: ./knowledge
#
# (optional) ExtraPath lists additional directories to prepend to the process
# extra_path:
#   - $HOME/.local/bin
#   - /usr/local/go/bin
#
# ShellExec configures the agent's ability to run shell commands.
shell_exec:
  # Enabled must be true for the agent to execute any shell commands.
  enabled: false
  # WorkingDir is the working directory for command execution. If
  # empty, the process's current directory is used.
  working_dir: ""
  # DeniedPatterns are substrings that cause a command to be rejected.
  # Checked before AllowedPrefixes. Example: "rm -rf /".
  denied_patterns:
    - rm -rf /
    - rm -rf /*
    - mkfs
    - dd if=
    - ':(){:|:&};:'
    - chmod -R 777 /
    - '> /dev/sda'
  # AllowedPrefixes restricts commands to those whose first token
  # matches one of these prefixes. An empty list means all commands
  # are allowed (subject to DeniedPatterns).
  allowed_prefixes: []
  # DefaultTimeoutSec is the maximum wall-clock time a command may
  # run before being killed. Default: 30.
  default_timeout_sec: 30
# DataDir is the root directory for SQLite databases (memory, facts,
# scheduler, checkpoints, and anticipations). Default: "./db".
data_dir: ./db
# TalentsDir is the directory containing talent markdown files that
# extend the system prompt. Default: "./talents".
talents_dir: ./talents
#
# (optional) PersonaFile is an optional markdown file that replaces the default
# persona_file: ./persona.md
#
# (optional) Context configures static context injection into the system prompt.
# context:
#   inject_files:
#     - ~/.agents.md
#     - ~/Thane/MEMORY.md
#
# (optional) Archive configures session archive behavior.
# archive:
#   metadata_model: qwen2.5-coder:32b
#   summarize_interval: 300
#   summarize_timeout: 60
#   session_idle_minutes: 30
#
# (optional) Extraction configures automatic fact extraction from conversations.
# extraction:
#   enabled: false
#   model: ""
#   min_messages: 2
#   timeout_seconds: 30
#
# (optional) Search configures web search providers.
# search:
#   default: searxng
#   searxng:
#     url: http://your-searxng:8080
#   brave:
#     api_key: your-brave-api-key
#
# (optional) Episodic configures episodic memory context injection (daily
# episodic:
#   daily_dir: ~/Thane/daily
#   lookback_days: 2
#   history_tokens: 4000
#   session_gap_minutes: 0
#
# (optional) Agent configures agent loop behavior, including orchestrator
# agent:
#   orchestrator_tools: []
#   delegation_required: false
#
# (optional) Delegate configures the thane_delegate tool's split-model execution.
# delegate:
#   profiles:
#     general:
#       tool_timeout: 3m
#       max_duration: 5m
#       max_iter: 15
#       max_tokens: 25000
#
# (optional) CapabilityTags defines named groups of tools and talents that
# capability_tags:
#   ha:
#     description: Home Assistant tools and sensors
#     tools:
#       - get_ha_states
#       - call_ha_service
#       - get_ha_history
#     always_active: true
#
# (optional) ChannelTags maps conversation source channels (e.g., "signal",
# channel_tags:
#   email:
#     - forge
#   signal:
#     - ha
#     - search
#
# (optional) MCP configures external MCP (Model Context Protocol) server
# mcp:
#   servers:
#     - name: my-mcp-server
#       transport: stdio
#       command: npx
#       args:
#         - -y
#         - '@modelcontextprotocol/server-example'
#       env: []
#       url: ""
#       headers: {}
#       include_tools: []
#       exclude_tools: []
#
# (optional) MQTT configures MQTT publishing for Home Assistant device discovery
# mqtt:
#   broker: mqtts://your-broker:8883
#   username: thane
#   password: your-mqtt-password
#   discovery_prefix: homeassistant
#   device_name: thane-ai-agent
#   publish_interval: 60
#   subscriptions:
#     - topic: homeassistant/+/+/state
#     - topic: frigate/events
#   telemetry:
#     enabled: true
#     interval: 60
#
# (optional) Person configures household member presence tracking. When Track
# person:
#   track:
#     - person.alice
#     - person.bob
#   devices:
#     person.alice:
#       - mac: AA:BB:CC:DD:EE:FF
#       - mac: 11:22:33:44:55:66
#   ap_rooms:
#     ap-bedroom: bedroom
#     ap-office: office
#
# (optional) Signal configures the Signal message bridge for inbound message
# signal:
#   enabled: true
#   command: signal-cli
#   account: "+15551234567"
#   args: []
#   rate_limit_per_minute: 10
#   session_idle_minutes: 30
#   routing:
#     model: ""
#     quality_floor: "6"
#     mission: conversation
#     delegation_gating: disabled
#   attachment_source_dir: ""
#   attachment_dir: ""
#   max_attachment_size: 0
#   handle_timeout: 10m
#
# (optional) Forge configures code forge integrations (GitHub, Gitea). When
# forge:
#   accounts:
#     - name: github
#       provider: github
#       token: ghp_your-token
#       owner: your-username
#       username: ""
#       url: https://api.github.com
#
# (optional) Email configures native IMAP email access. When configured, Thane
# email:
#   bcc_owner: ""
#   poll_interval: 0
#   accounts:
#     - name: primary
#       imap:
#         host: imap.example.com
#         port: 993
#         username: thane@example.com
#         password: your-email-password
#         tls: true
#       smtp:
#         host: smtp.example.com
#         port: 587
#         username: thane@example.com
#         password: your-email-password
#         starttls: false
#       default_from: Thane <thane@example.com>
#       sent_folder: ""
#
# (optional) Identity configures the agent's own contact identity for vCard
# identity:
#   contact_name: Thane
#
# (optional) Attachments configures content-addressed attachment storage.
# attachments:
#   store_dir: ~/Thane/attachments
#   vision:
#     enabled: true
#     model: llava:latest
#     prompt: ""
#     timeout: 30s
#
# (optional) Provenance configures git-backed file storage with SSH signature
# provenance:
#   path: ~/Thane/identity
#   signing_key: ~/.ssh/id_ed25519
#
# (optional) StateWindow configures the rolling window of recent Home Assistant
# state_window:
#   max_entries: 50
#   max_age_minutes: 30
#
# (optional) Unifi configures the UniFi network controller connection for
# unifi:
#   url: https://192.168.1.1
#   api_key: your-unifi-api-key
#   poll_interval: 30
#
# (optional) Prewarm configures context pre-warming for cold-start loops.
# prewarm:
#   enabled: false
#   max_facts: 10
#   archive:
#     enabled: false
#     max_results: 3
#     max_bytes: 4000
#
# (optional) Media configures the media transcript retrieval tool. When yt-dlp
# media:
#   yt_dlp_path: ""
#   cookies_file: ""
#   cookies_from_browser: ""
#   subtitle_language: en
#   max_transcript_chars: 50000
#   whisper_model: ""
#   transcript_dir: ""
#   summarize_model: ""
#   feed_check_interval: 3600
#   max_feeds: 50
#   analysis:
#     default_output_path: ~/Sync/Vault/Media
#     database_path: ""
#
# (optional) Metacognitive configures the perpetual metacognitive attention loop.
# metacognitive:
#   enabled: false
#   state_file: metacognitive.md
#   min_sleep: 2m
#   max_sleep: 30m
#   default_sleep: 10m
#   jitter: 0.2
#   supervisor_probability: 0.1
#   router:
#     quality_floor: 3
#   supervisor_router:
#     quality_floor: 8
#
# (optional) OpenClaw configures the thane:openclaw Ollama profile, which
# openclaw:
#   workspace: ~/Thane/openclaw
#   skills_dirs:
#     - ~/Thane/openclaw/skills
#   max_file_chars: 20000
#
# (optional) Debug configures diagnostic options for inspecting the assembled
# debug:
#   demo_loops: false
#
# (optional) Timezone is the IANA timezone for the household (e.g.,
# timezone: America/Chicago
#
# (optional) Pricing maps model names to their per-million-token costs (USD).
# pricing:
#   claude-opus-4-20250514:
#     input_per_million: 15.0
#     output_per_million: 75.0
#
# Logging configures log output, rotation, and format. Thane manages
# its own log files — no external logrotate required.
logging:
  # Dir is the directory for log files. Relative paths are resolved
  # from the working directory (typically ~/Thane). Defaults to "logs"
  # when omitted. Set to an explicit empty string (dir: "") to disable
  # file logging (output goes to stdout only).
  dir: logs
  # Level sets the minimum log level. Valid values: trace, debug,
  # info, warn, error. Default: info.
  level: info
  # Format sets the log output format. "json" produces one JSON
  # object per line; "text" produces human-readable key=value pairs.
  # Default: json.
  format: json
  # Compress enables gzip compression of rotated log files.
  # Default: true.
  compress: true
  # RetentionDays controls how many days DEBUG and TRACE log index
  # entries are kept. Entries at INFO and above are kept indefinitely.
  # Default: 7. Set to 0 to disable pruning (keep everything).
  retention_days: 7
  # RetainContent enables content retention in the log index database.
  # When true, system prompts (deduplicated by SHA-256 hash), tool call
  # arguments/results, and request/response content are persisted to
  # logs.db alongside the existing log index. Default: false.
  retain_content: false
  # MaxContentLength is the maximum number of characters retained per
  # tool result or message body. Longer content is truncated. This
  # bounds storage growth while preserving enough for diagnostics.
  # Default: 4096. Set to 0 for unlimited.
  max_content_length: 4096
  # ContentArchiveDays is the age threshold in days for archiving
  # log_request_content rows to JSONL flat files. Rows older than
  # this are exported to ContentArchiveDir and removed from logs.db.
  # Default: 90. Set to 0 to disable archival.
  content_archive_days: 90

#
# (optional) LogLevel is deprecated; use Logging.Level instead.
# log_level: ""
#
# (optional) LogFormat is deprecated; use Logging.Format instead.
# log_format: ""
